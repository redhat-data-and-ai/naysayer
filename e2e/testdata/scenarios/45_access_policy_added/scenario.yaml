name: "access_policy added to product.yaml - requires manual review"
description: "Adding access_policy to a product.yaml should trigger manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "access_policy changes require manual review"

mr_metadata:
  title: "Add access_policy to analytics marts"
  author: "testuser"
  source_branch: "feature/add-access-policy"
  target_branch: "main"
