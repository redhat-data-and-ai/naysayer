name: "Consumer group added without groups file"
description: "Adding a consumer_group consumer without a corresponding YAML file in the groups/ folder should require manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "Consumer group file(s) not found"

mr_metadata:
  title: "Add consumer group to payments product"
  author: "testuser"
  source_branch: "feature/add-payments-consumer"
  target_branch: "main"
