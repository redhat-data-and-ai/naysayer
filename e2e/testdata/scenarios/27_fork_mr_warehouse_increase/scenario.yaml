name: "Fork MR — source fetch regression"
description: "Source branch only on fork project; validation must fetch from fork project and require manual review for uncovered file type"

expected:
  decision: ManualReview
  reason: "Uncovered changes require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "No validation rules configured for this file type"
    - "Uncovered changed lines"

mr_metadata:
  title: "Scale up warehouse for increased load (fork)"
  author: "testuser"
  source_branch: "feature/warehouse-scale-up"
  target_branch: "main"
  fork_source_project_id: 9999
