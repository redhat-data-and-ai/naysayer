name: "Fork MR — warehouse size increase"
description: "Source branch only on fork project; section validation must fetch from fork (regression for MR 7309-style bug)"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  rules_evaluated:
    - name: warehouse_rule
      section: warehouses
      decision: ManualReview
      reason: "Warehouse size increase detected"

  comment_contains:
    - "⚠️ **Manual review required**"
    - "Warehouse size increase detected"
    - "user warehouse"

mr_metadata:
  title: "Scale up warehouse for increased load (fork)"
  author: "testuser"
  source_branch: "feature/warehouse-scale-up"
  target_branch: "main"
  fork_source_project_id: 9999
