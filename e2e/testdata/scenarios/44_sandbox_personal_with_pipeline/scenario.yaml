name: "Sandbox Personal - With Unstructured Pipeline File"
description: "Adding unstructured-data-pipeline.yaml to existing sandbox Personal product should auto-approve"

expected:
  decision: Approve
  approved: true

  comment_contains:
    - "Auto-approved"
    - "Unstructured data pipeline configuration in sandbox personal environment"

mr_metadata:
  title: "Add unstructured data pipeline configuration"
  author: "alice@company.com"
  source_branch: "feature/add-pipeline-config"
  target_branch: "main"
