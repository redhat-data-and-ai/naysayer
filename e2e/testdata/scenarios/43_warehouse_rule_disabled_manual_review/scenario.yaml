name: "Warehouse rule disabled with changes requires manual review"
description: "When warehouse_rule is disabled in local rules, warehouse changes must still require manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"

mr_metadata:
  title: "Warehouse change with warehouse rule disabled"
  author: "testuser"
  source_branch: "feature/warehouse-rule-disabled"
  target_branch: "main"
