name: "New product (preprod) + Existing product (dev) modification"
description: "New product.yaml in preprod plus existing dev product with consumer change - TOC should trigger manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "TOC approval"

mr_metadata:
  title: "Onboard EBSHR to preprod and update dev consumers"
  author: "testuser"
  source_branch: "feature/ebshr-preprod-onboarding"
  target_branch: "main"
