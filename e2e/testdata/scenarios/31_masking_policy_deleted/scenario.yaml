name: "Deleted masking policy - requires manual review"
description: "Deleting a masking policy file removes data protection and requires manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "Deletion requires manual review: masking_files"

mr_metadata:
  title: "Delete masking policy"
  author: "testuser"
  source_branch: "feature/delete-masking-policy"
  target_branch: "main"

