name: "Sandbox Unstructured - Product Config Update Only"
description: "Updating only unstructured-data-product.yaml (not pipeline) should auto-approve"

expected:
  decision: Approve
  approved: true

  comment_contains:
    - "Auto-approved"

mr_metadata:
  title: "Update unstructured product configuration"
  author: "alice@company.com"
  source_branch: "feature/update-config"
  target_branch: "main"
