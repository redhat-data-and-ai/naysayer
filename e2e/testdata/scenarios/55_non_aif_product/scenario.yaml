name: "Non-AIF Product - Falls Back to General Rules"
description: "UnstructuredDataProduct with name not starting with 'aif-' should NOT trigger sandbox rules - falls back to general validation which requires manual review"

expected:
  decision: ManualReview
  approved: false

  # Should NOT see sandbox rule messages since path pattern won't match
  comment_not_contains:
    - "sandbox UnstructuredDataProduct"
    - "sandbox rules"

mr_metadata:
  title: "Update non-aif unstructured product"
  author: "alice@company.com"
  source_branch: "feature/update-non-aif-product"
  target_branch: "main"
