name: "Missing expected rule triggers fallback"
description: "Configured enabled rule missing from registry should trigger generic manual-review fallback"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "'missing_metadata_rule' was not evaluated"

mr_metadata:
  title: "Fallback for missing expected rule"
  author: "testuser"
  source_branch: "feature/fallback-missing-rule"
  target_branch: "main"
