source_crawler_config:
  type: "google_drive"
  googleDriveConfig:
    folder_ids: []

destination_syncer_config:
  type: "s3"
  s3DestinationConfig:
    bucket: "dataverse-sandbox-unstructured"
    prefix: "aif-invaliddrive/destination/"
