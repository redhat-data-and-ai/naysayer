apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: naysayer
  namespace: ddis-asteroid--naysayer
  labels:
    shard: internal
  annotations:
    haproxy.router.openshift.io/timeout: 180s
spec:
  host: naysayer-ddis-asteroid--naysayer.apps.int.spoke.preprod.us-west-2.aws.paas.redhat.com
  to:
    kind: Service
    name: naysayer
    weight: 100
  port:
    targetPort: https
  tls:
    termination: edge
  wildcardPolicy: None
